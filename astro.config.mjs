import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
  // GitHub Pages project site (username.github.io/<repo>/)
  site: 'https://crudualexandra.github.io/Magazin-Tipografia/',
  
  base: '/Magazin-Tipografia/',
});
